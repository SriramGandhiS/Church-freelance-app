import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Scaffolds & Surfaces ────────────────────────────────────
  static const Color scaffold        = Color(0xFFF8F9FD);
  static const Color surface         = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFF0F4FF);

  // ── Baby Blue — Primary Accent ─────────────────────────────
  static const Color primary         = Color(0xFF4A7FBD);
  static const Color primaryLight    = Color(0xFFDBEAF8);
  static const Color primaryLighter  = Color(0xFFEFF5FC);

  // ── Soft Lavender — Secondary Accent ───────────────────────
  static const Color lavender        = Color(0xFF7B6FA6);
  static const Color lavenderLight   = Color(0xFFEDE9F8);
  static const Color lavenderLighter = Color(0xFFF5F3FC);

  // ── Pale Blush — Warm Accent ───────────────────────────────
  static const Color blush           = Color(0xFFC47A7A);
  static const Color blushLight      = Color(0xFFF8EDED);

  // ── Sage Green — Success / Active ──────────────────────────
  static const Color sage            = Color(0xFF5A8A6A);
  static const Color sageLight       = Color(0xFFE6F2EA);

  // ── Text ────────────────────────────────────────────────────
  static const Color textPrimary     = Color(0xFF1A2440);
  static const Color textSecondary   = Color(0xFF3D4E6B);
  static const Color textMuted       = Color(0xFF7A8AAD);
  static const Color textHint        = Color(0xFFABB5CC);
  static const Color textOnDark      = Color(0xFFFFFFFF);
  static const Color textOnPastel    = Color(0xFF2A3A5C);

  // ── Borders & Dividers ──────────────────────────────────────
  static const Color border          = Color(0xFFDDE3EE);
  static const Color divider         = Color(0xFFECEFF7);
  static const Color borderFocus     = Color(0xFF4A7FBD);

  // ── Status ──────────────────────────────────────────────────
  static const Color success         = Color(0xFF5A8A6A);
  static const Color successLight    = Color(0xFFE6F2EA);
  static const Color warning         = Color(0xFF9B7D3A);
  static const Color warningLight    = Color(0xFFF7F0DC);
  static const Color error           = Color(0xFFA84040);
  static const Color errorLight      = Color(0xFFF8ECEC);

  // ── Hero Gradient ───────────────────────────────────────────
  static const Color bannerScrimTop  = Color(0x00000000);
  static const Color bannerScrimBot  = Color(0xCC1A2440);
}
